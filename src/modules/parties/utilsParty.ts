
import { User } from '../auth/authService';
import { Doc } from '../data/models';






export const docIsProject = (doc:Doc):boolean => {
  return false;
}

export const canEdit = (doc:Doc, user:User) => {
  
}